# Auto Claude data directory
.auto-claude/
.worktrees/
.claude_settings.json

# Environment variables - contains sensitive credentials
# Note: .env.example is intentionally NOT ignored (template for setup)
.env

# API tokens and access files - NEVER commit these
access.txt
